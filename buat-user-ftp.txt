<?php

$apiUrl = "https://digitalpro.my.id/api/ftp/add_ftp";

$data = [
    "user"    => "administrator",
    "pass"    => "Marlin22331",
    "homedir" => "."
];

$ch = curl_init($apiUrl);

curl_setopt_array($ch, [
    CURLOPT_RETURNTRANSFER => true,
    CURLOPT_POST => true,
    CURLOPT_POSTFIELDS => http_build_query($data),
]);

$response = curl_exec($ch);

if (curl_errno($ch)) {
    echo "Error: " . curl_error($ch);
} else {
    echo $response;
}

curl_close($ch);
